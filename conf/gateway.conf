# MQTT 网关配置 — 预留，M2/M3 阶段使用
# 连接第三方 MQTT Broker，用于车况数据上报

[mqtt]
host = 127.0.0.1
port = 1883
client_id = smart_car_engine_001
topic_prefix = vehicle/001/
qos = 1
keepalive = 60

[can]
interface = vcan0
