# AI 语音控制模块配置
# M2 阶段使用，通过 DeepSeek API 实现自然语言车控

[api]
url = https://api.deepseek.com/v1/chat/completions
# 演示用 key,如担心泄露可改用环境变量 DEEPSEEK_API_KEY(优先级更高)
key = sk-4f08bcae0ccc47e0b43de2cb8b8bb145
model = deepseek-chat
timeout_ms = 3000
max_retries = 1

[safety]
# 行驶中禁止操作列表
speed_threshold_kmh = 5
forbidden_when_moving = door,window

# AI 不可控字段列表
ai_denied = gear

[uds]
socket_path = /tmp/car_core.sock
